# 🎶 Textual Karaoke

A simple Python-based karaoke player that syncs lyrics with the rhythm of a song using `.lrc` files.  
Built with ❤️ using Python and Colorama.

## Features
- Parses `.lrc` files and displays lyrics in real time.
- Supports multiple “moods” (romantic, dance, heartbreak).
- Works directly in your terminal.

## Usage
1. Place your `.lrc` file in the same folder as `lyric_player.py`.
2. Run:
   ```bash
   python lyric_player.py
